# <功能名> — 领域模型

> 导航入口：`../../00-入口/文档导航.md`。所属阶段：阶段 1 需求理清。任务编号：1.3。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：收敛型（批次内）**。命名 `领域模型.md`（无序号单例）。
> 表达「**本批次结束时的**当前真相」，随本轮上游产物演进**原地更新**，不另建版本文件。
> **非首批次开工时，应先从上一批次的同名文件继承**，再在本批次内继续演进——这样「当前真相」始终落在最新批次，历史批次各自冻结为当时快照。

> **职责边界（与 `跨端共享需求.md` 的唯一分工口径）**
> 本文件回答「**领域里有什么**」：术语叫什么、实体有哪些字段、字段什么含义与类型、什么条件恒成立、枚举有哪些取值。
> **两档都产出**（含单端开发）；字段的语义与类型定义以本文件为家。
> `跨端共享需求.md` 回答「**两端怎么写才一致**」：双端字面映射、接口调用姿势、跨端口径、错误语义对齐——**仅两端时产出**，
> 它承载**跨端契约的权威裁定**，且只登记跨端维度，**不重抄本文件的语义与类型**。
> 两者按**列**分工，不按**行**重复：同一个字段名会同时出现在两份文件里，但说的不是同一件事。

## 1. 术语表

**一个概念只能有一个名字，一个名字只能指一个概念。** 代码命名、文档、测试、UI 文案都取自本表。

| 术语 | 权威名（代码名） | 定义 |
|---|---|---|
| | | |

<!--
填写要求：
- 「权威名」是**唯一拼写**：本文件、`跨端共享需求.md`、代码、测试都引用它。
- 权威名用类型/字段的命名惯例（类型 UpperCamelCase、字段与函数 lowerCamelCase）。
- 容易混淆的近义概念必须拆成两行（「阅读进度」与「阅读位置」若语义不同就分别定义）。
- 「禁止使用的同义词」不在本表——那是**跨端命名纪律**，见 `跨端共享需求.md` 的「命名规范」节。
- 与既有领域模型冲突时，先改本表再改下游，不要在各处就地打补丁。
-->

## 2. 实体

### <实体名>

| 字段名 | 语义 | 类型口径 | 可选性 | 取值约束 | 默认值 |
|---|---|---|---|---|---|
| | | | | | |

**类型口径词表**（本文件与 `跨端共享需求.md` 共用这一套，**只用这几种**，不写语言类型如 `String` / `val` / `Optional`）：

| 口径 | 含义 | 备注 |
|---|---|---|
| `字符串` | 文本 | 编码与规范化口径在跨端档的「一致性铁律」 |
| `整数` | 无小数 | 位宽与符号性写进「取值约束」 |
| `浮点` | 有小数 | 精度写进「取值约束」 |
| `布尔` | 真 / 假 | |
| `时间点` | 绝对时刻 | 时区与精度口径在跨端档 |
| `时长` | 时间跨度 | 单位口径在跨端档 |
| `枚举` | 取值域有限 | 取值范围见本文件第 4 节 |
| `列表<…>` | 有序集合 | 顺序与去重语义写进「取值约束」 |
| `可选<…>` | 可能不存在 | 与「空值」的区别见「可选性」列 |
| `字节串` | 二进制 | |
| `标识` | 唯一标识 | 生成方式与作用域写进「取值约束」 |

**「可选性」三态**（必须显式声明，不得靠约定）：

| 取值 | 含义 |
|---|---|
| 必填 | 一定存在；缺失即为非法数据 |
| 可空 | 存在但语义为「无」 |
| 有默认值 | 缺省时取「默认值」列的值 |

> **禁止用哨兵值表示「无」**：空字符串、`0`、`-1`、`"N/A"` 都不算「无」，必须用 `可选<…>`。

**实体关系**（实体之间怎么关联）：

| 关系 | 基数 | 说明 |
|---|---|---|
| | 一对一 / 一对多 / 多对多 | |

## 3. 不变量

领域规则中**必须恒成立**的条件。跨端场景下两端都必须成立（一致性由 `跨端共享需求.md` 的「共享范围」节纳入）。

| 编号 | 不变量 | 违反时的行为 |
|---|---|---|
| V1 | | |

<!--
填写要求：
- 每条必须**可验证**（能写出测试用例），不写「应当合理」这类无法判定的表述。
- 「违反时的行为」必须写明：拒绝 / 回退到某值 / 静默忽略——不允许留白，否则两端各自决定如何处理。
- 编号 V1、V2… 在本文件内唯一；`跨端共享需求.md` 引用时沿用同一编号。
-->

## 4. 枚举与取值域

| 枚举名 | 取值 | 含义 | 用于哪些字段 |
|---|---|---|---|
| | | | |

<!--
本表只承载**取值语义**（这个取值是什么意思）。
跨端相关的内容——**线格式名、双端代码字面、未知值处理、冻结/可增长**——在 `跨端共享需求.md` 的「枚举与取值域」节。
单端开发时只产出本文件，故取值本身必须在此定义完整。
-->
