# <功能名> — 共享层

> 导航入口：`../00-入口/文档导航.md`。所属阶段：阶段 2 架构设计。任务编号：2.6。
> 仅当「两端都要开发」时产出；这是跨端共享的核心契约，两端落地层各自引用，不得两端各自独立建模。

## 1. 领域模型

<!-- 共享领域实体、值对象、不变量（两端语义一致，允许语言级不同但语义等价） -->

## 2. 数据契约

<!-- 跨端共享的数据结构/序列化契约（字段、类型、边界、默认值）——这是共享层最硬的约束 -->

## 3. 不变量清单

<!-- 跨端必须一致的业务不变量（输入校验规则、边界条件、错误语义） -->
