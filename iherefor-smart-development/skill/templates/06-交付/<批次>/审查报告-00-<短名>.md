# <功能名> — 审查报告

> 导航入口：`../../00-入口/文档导航.md`。所属阶段：阶段 6 交付。任务编号：6.2。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：追加型（批次内）**。命名 `审查报告-NN-<短名>.md`（NN 两位数字，从 00 起递增）。
> **每次新增内容 → 新建下一个编号的文件**，不得在本文件上增删改（这是追加式演进，历史文件保持不可变）。
> 用 code-review（Standards × Spec 双轴）。绿地工程/共享仓库/小代码场景可降级，须在此说明原因。

## 1. 审查结论

<!-- 双轴结论 + 发现的问题清单 -->

## 2. 降级说明

<!-- 若未跑 code-review 或降级，说明原因（如：绿地无 commit 基线 / 共享仓库 / 作者本人刚 TDD 写完） -->
