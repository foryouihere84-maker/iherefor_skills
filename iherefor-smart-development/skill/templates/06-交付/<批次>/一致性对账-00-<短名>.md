# <功能名> — 一致性对账

> 导航入口：`../../00-入口/文档导航.md`。所属阶段：阶段 6 交付（交付门）。任务编号：6.3。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：追加型（批次内）**。命名 `一致性对账-NN-<短名>.md`（NN 两位数字，从 00 起递增）。
> **每次新增内容 → 新建下一个编号的文件**，不得在本文件上增删改（这是追加式演进，历史文件保持不可变）。
> 本文件是「代码 ↔ 文档」对账产物，交付门前必须填写，缺它无法通过校验。
> 对账基线 = 阶段 2/3/4 已确认的契约文档（含原型）；同步点映射见 `references/代码文档同步.md`。

## 1. 同步点逐项核对

<!-- 按 代码文档同步.md 第 2 节映射表，逐项核对「文档宣称 ↔ 代码实现」。每项给出判定：已同步 / 漂移 / 需复核 -->

| 同步点 | 文档产物 | 代码侧实际 | 判定 | 处理 |
|---|---|---|---|---|
| 实体/字段 | `01-需求/<批次>/领域模型.md` | | | |
| 不变量 | `01-需求/<批次>/领域模型.md` | | | |
| **页面结构 / 屏数 / 交互状态** | **`02-原型/<批次>/原型说明.md`** | | | |
| **视觉 token / 配色 / 字号 / 间距** | **`02-原型/<批次>/原型说明.md`** | | | |
| 数据契约 | `03-架构/跨端共享层.md`（如有） | | | |
| 签名/导航/资源 | `04-落地计划/<批次>/变更清单-*.md` | | | |
| 依赖 | `04-落地计划/<批次>/变更清单-*.md` | | | |
| 边界/非目标 | `01-需求/<批次>/需求-*.md` | | | |
| 测试 seam | `05-实现与测试/<批次>/测试计划.md` | | | |

## 2. 漂移项清单

<!-- 列出本次对账发现的漂移项，及其处理结果。原则：要么回写文档、要么回退代码，不允许带漂移交付 -->

- [ ] 无漂移项（直接勾选本行，其余行删除）

## 3. 结论

<!-- 最终结论：全部已同步 / 存在需人工复核项（列明） -->
