# <功能名> — 任务拆分 tickets

> 导航入口：`../../00-入口/文档导航.md`。所属阶段：阶段 4 工程落地计划。任务编号：4.4。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：收敛型（批次内）**。命名 `任务拆分.md`（无序号单例）。
> 表达「**本批次结束时的**当前真相」，随本轮上游产物演进**原地更新**，不另建版本文件。
> **非首批次开工时，应先从上一批次的同名文件继承**，再在本批次内继续演进——这样「当前真相」始终落在最新批次，历史批次各自冻结为当时快照。
> 把变更拆成可派发的 tracer-bullet tickets，每条 ticket 独立可测、可交付。

## 1. Tickets

### T-1 <ticket 名>

- 依赖：
- 范围：
- 完成标准：

### T-2 <ticket 名>

- 依赖：
- 范围：
- 完成标准：
