# <功能名> — 变更清单

> 导航入口：`../../00-入口/文档导航.md`。所属阶段：阶段 4 工程落地计划。任务编号：4.2。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：追加型（批次内）**。命名 `变更清单-NN-<短名>.md`（NN 两位数字，从 00 起递增）。
> **每次新增内容 → 新建下一个编号的文件**，不得在本文件上增删改（这是追加式演进，历史文件保持不可变）。
> 本文件是闸门 c 的交付物，批准后才可写生产代码。动既有工程是高风险操作。

## 1. 文件变更

<!-- 列出要新增/修改/删除的每个文件，说明变更性质 -->

| 文件 | 操作 | 说明 |
|---|---|---|

## 2. 依赖

<!-- 新增/升级的依赖（含版本） -->

## 3. 签名/导航/资源变更

<!-- 改动的公开接口签名、导航结构、资源文件 -->
