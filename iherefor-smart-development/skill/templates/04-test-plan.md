# <功能名> — 测试计划

> 导航入口：`../00-入口/文档导航.md`。所属阶段：阶段 4 TDD 落地。任务编号：4.1。
> 测试栈与 seam 默认值见 `config/mobile-tdd.json`；两端细则见 `references/ios-tdd.md` / `references/android-tdd.md`。

## 1. 测试栈

<!-- iOS 用 XCTest/Swift Testing；Android 用 JUnit+MockK+Turbine；此处定本功能实际采用 -->

## 2. seam 位置

<!-- 测在哪个接缝：领域层/用例/ViewModel；哪些外部依赖需要 mock -->

## 3. should 用例

<!-- 用 shouldXxx 命名，列出测试类 + 用例 + Mock + 断言 -->

| should 用例 | Mock | 断言 |
|---|---|---|
| should... | | |
