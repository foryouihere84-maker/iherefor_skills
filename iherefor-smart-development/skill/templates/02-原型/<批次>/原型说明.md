# <功能名> — 原型说明

> 导航入口：`../../00-入口/文档导航.md`。开发/评审先读导航，再进本文件。
> 所属阶段：阶段 2 原型设计。任务编号：2.1 / 2.3。闸门：🔒 p（原型门）。
> **所属批次：`<批次>`**（= `NN-<需求名>`，如 `01-阅读进度续读`）。批次目录名在 01/02/04/05/06 各阶段间**必须同名对齐**。
> **产物类型：收敛型（批次内）**。命名 `原型说明.md`（无序号单例）。
> 表达「**本批次结束时的**当前真相」，随本轮上游产物演进**原地更新**，不另建版本文件。
> **非首批次开工时，应先从上一批次的同名文件继承**，再在本批次内继续演进——这样「当前真相」始终落在最新批次，历史批次各自冻结为当时快照。
> 配套原型文件：`./原型图.html`（双击即在浏览器打开，零依赖、零构建）。
> 本文件新增/修改了页面清单、交互状态或视觉 token 时，须同步更新 `原型图.html`；反之亦然（见本 skill 的 `references/原型绘制.md` 第 10 节）。

## 1. 页面清单

<!-- 一屏一行。屏 id 必须是【中文语义名】，表达"哪个功能的哪一屏"，与 原型图.html 的 data-screen 逐字一致。 -->
<!-- 「对应故事」列来自 01-需求/用户故事-*.md 的编号；倒查不到故事的屏 = 范围蔓延，必须处理。 -->

| 屏 id | 屏名 | 对应故事 | 用途（一句话） |
|---|---|---|---|
| `<中文屏id>` | <中文屏名> | US-x | <这一屏解决什么> |

## 2. 关键交互

<!-- 只写"屏与屏之间怎么走"，不写实现细节。每条：从哪屏、什么动作、到哪屏、带什么参数。 -->
<!-- 交互原型要求：这里列的每条路径，在 原型图.html 里都必须真的点得动。 -->

| 从 | 动作 | 到 | 说明 |
|---|---|---|---|
| `<屏id>` | <用户动作> | `<屏id>` | <参数 / 前置条件 / 失败分支> |

## 3. 状态覆盖

<!-- 对照 references/原型绘制.md 第 7 节的清单逐屏过。不适用的写"不适用"并给理由，不要留空。 -->

| 屏 id | 空态 | 加载中 | 有数据 | 错误 / 拒绝 | 边界 / 越界 | 极值 |
|---|---|---|---|---|---|---|
| `<屏id>` | ✅ / ❌ / 不适用 | … | … | … | … | … |

## 4. 视觉稿规格

<!-- 视觉稿的"可照抄数值"。必须与 原型图.html 里 :root 下的 CSS 自定义属性一一对应。 -->
<!-- 两端都要时，两端视觉语言不同的项分开列（见 references/原型绘制.md 第 5.3 节）。 -->

### 4.1 色彩

| 用途 | Token | 值（hex） |
|---|---|---|
| 主色 | `--color-primary` | # |
| 主色浅底 | `--color-primary-soft` | # |
| 背景 | `--color-bg` | # |
| 卡片面 | `--color-surface` | # |
| 分隔线 | `--color-line` | # |
| 主文字 | `--color-text` | # |
| 次文字 | `--color-text-dim` | # |
| 危险色 | `--color-danger` | # |
| 成功色 | `--color-success` | # |

### 4.2 字号 / 间距 / 圆角

| 类别 | Token | 值 |
|---|---|---|
| 大标题 | `--font-title-lg` | <pt/px> / <字重> |
| 标题 | `--font-title` | … |
| 正文 | `--font-body` | … |
| 辅助 | `--font-caption` | … |
| 页面边距 | `--space-page` | … |
| 卡片内边距 | `--space-card` | … |
| 列表行高 | `--size-row` | … |
| 圆角（卡片） | `--radius-card` | … |
| 圆角（按钮） | `--radius-button` | … |

### 4.3 图标

- 用哪套：<系统内置 / 自绘内联 SVG>
- 出现位置：<顶部操作、底部导航、状态提示 …>

### 4.4 跨端视觉语言差异

<!-- 两端都要时填；单端填"不适用"。 -->

| 项 | iOS | Android |
|---|---|---|
| 顶部导航 | <大标题 / 内联标题> | <TopAppBar> |
| 底部导航 | <标签栏> | <底部导航 + FAB> |
| 其余差异 | … | … |

## 5. 设计取舍

<!-- 变体决策与结构取舍的"为什么"。胜出的是哪个、偷了别的变体哪一块、放弃了什么。 -->

- **保真度档位**：<V2 视觉稿（默认） / V1 / 个别局部 V0 及其原因>
- **变体决策**：<胜出变体 + 理由；若单变体则写"方向已定，不做多变体"的理由>
- **结构取舍**：<被放弃的方案与放弃原因>
- **跨端一致性**：<两端都要时，哪些屏/状态/文案必须两端一致，哪些允许分叉>

## 6. 走查记录

<!-- 闸门 p 的输入：逐屏走查，不是"看一眼觉得还行"。走查发现的问题与处理结果都要留痕。 -->

| 屏 id | 覆盖的故事 / 场景 | 覆盖状态 | 结论 |
|---|---|---|---|
| `<屏id>` | US-x 场景（…） | <空 / 加载 / 有数据 / 错误 / 越界 / 极值> | 通过 / 待改 |

**走查四问**（逐条回答）：

1. 每屏都能倒查到故事吗？<答>
2. 每屏的状态覆盖齐了吗？<答>
3. 跨端一致的部分画出来了吗？<答>
4. 有没有想当然画出来、与 Spec 冲突的屏？<答>

**结论**：<通过 → 提请闸门 p 确认；待改 → 列出待改项与归属>

## 7. 实现回写约定

<!-- 阶段 3 起的漂移回写记录。改代码时若动了页面结构/屏数/交互状态/视觉 token，在此登记。 -->

| 发生阶段 | 变更内容 | 回写到本文件哪一节 | 是否已回写 |
|---|---|---|---|
| — | — | — | — |
