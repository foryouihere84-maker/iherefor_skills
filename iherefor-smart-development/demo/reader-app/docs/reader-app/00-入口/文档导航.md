# 阅读应用 — 文档导航与当前结论

<!-- 本模板对应产物文件：docs/<feature>/00-入口/文档导航.md（见 templates/模板说明.md 清单） -->

> 更新时间：2026-09-17
> **作用域：全局单例**（不属任何批次）。
> **产物类型：收敛型（全局）**。命名 `文档导航.md`（无序号单例）。
> 目的：把本功能的全部产出物收敛成**一个读取入口**，明确批次清单、当前批次、冲突优先级与子 agent 分派口径。

## 1. 使用规则

开发、评审、子 agent 分派时先读本文，再进入具体产物文件。
冲突处理：主线结论（原型/架构/共享层/变更清单）优先于早期草案；若发现拆分产物与本文冲突，先停下报告，不自行裁决。
**批次定位**：先看第 3 节的批次清单确定本次要做的是哪个批次，再按第 5 节的读取顺序进该批次的产物。

## 2. 当前核心结论

| 主题 | 当前结论 | 权威产物 |
|---|---|---|
| **当前批次** | `01-多格式书架与续读`（已交付） | 第 3 节批次清单 |
| 端覆盖 | iOS + Android 双端，须先定共享层 | `01-需求/01-多格式书架与续读/跨端共享需求.md` |
| 支持格式 | EPUB + PDF + TXT | `01-需求/01-多格式书架与续读/需求-00-多格式书架与续读.md` |
| 档位（tier） | full（跨端 + 新领域 + 涉及持久化 + 新增依赖） | `config/tier.json` |
| 功能短名 | reader-app | 本导航 |
| 领域不变量 | 格式不可变 / 进度与格式兼容 / sourceUri 去重 / 越界回退 | `01-需求/01-多格式书架与续读/领域模型.md` |
| 界面共识 | 5 屏 12 态，列表式书架为胜出变体 | `02-原型/01-多格式书架与续读/原型说明.md` |
| 视觉语言 | iOS 大标题导航条 / Android 顶栏 + Material 悬浮按钮，同一份 HTML 内切换 | `02-原型/01-多格式书架与续读/原型图.html` |
| 架构基线 | 单向数据流 + BookReaderService 按格式分派；零第三方依赖 | `03-架构/方案草案.md` |

## 3. 批次清单（本功能的全部需求迭代）

> **一次需求迭代 = 一个 `<NN-需求名>` 批次目录**，NN 两位数字从 `01` 起**连续递增**。
> 各阶段（`01-需求`/`02-原型`/`04-落地计划`/`05-实现与测试`/`06-交付`）下的批次目录**必须同名对齐**。
> 本文是批次清单的**唯一权威索引**——新增批次必须在此追加一行。

| 批次 | 需求主题 | 状态 | 起止 | 备注 |
|---|---|---|---|---|
| `01-多格式书架与续读` | 多格式（EPUB/PDF/TXT）导入、书架列表、阅读与进度续读、删除 | 已交付 | 2026-09-17 ~ 2026-09-17 | 首版双端落地；full 档 |

> **新增一次需求 = ① 在 5 个阶段各建同名批次目录（如 `01-需求/02-阅读进度续读/` … `06-交付/02-阅读进度续读/`）→ ② 本表追加一行 → ③ 在 `99-来源与日志/运行日志.jsonl` 记 `new_batch` 事件。**
> 批次一旦交付即**冻结为历史快照**，后续修订走新批次，不回头改旧批次。

### 3.1 批次内产物路径规则

`<批次>` 即 `NN-<需求名>`（本工程当前为 `01-多格式书架与续读`）：

| 阶段 | 批次内产物 | 类型 |
|---|---|---|
| `01-需求/<批次>/` | `需求-NN-<短名>.md`、`用户故事-NN-<短名>.md` | 追加型 |
| | `领域模型.md`、`跨端共享需求.md` | 收敛型 |
| `02-原型/<批次>/` | `原型说明.md`、`原型图.html` | 收敛型 |
| `04-落地计划/<批次>/` | `变更清单-NN-<短名>.md` | 追加型 |
| | `架构审计.md`、`回滚方案.md`、`任务拆分.md` | 收敛型 |
| `05-实现与测试/<批次>/` | `测试计划.md` | 收敛型 |
| `06-交付/<批次>/` | `编译报告-NN-<短名>.md`、`审查报告-NN-<短名>.md`、`一致性对账-NN-<短名>.md`、`交付报告-NN-<短名>.md` | 追加型 |

本批次实际落盘：

| 类型 | 序号 | 文件 | 主题 | 新增时间 |
|---|---|---|---|---|
| 需求 | 00 | `01-需求/01-多格式书架与续读/需求-00-多格式书架与续读.md` | 多格式书架与续读（首版需求基线） | 2026-09-17 |
| 用户故事 | 00 | `01-需求/01-多格式书架与续读/用户故事-00-多格式书架与续读.md` | 导入/书架/阅读/进度/删除 5 条故事 | 2026-09-17 |
| 变更清单 | 00 | `04-落地计划/01-多格式书架与续读/变更清单-00-首版落地.md` | 首版双端落地 | 2026-09-17 |
| 编译报告 | 00 | `06-交付/01-多格式书架与续读/编译报告-00-首版.md` | 首版双端编译 | 2026-09-17 |
| 审查报告 | 00 | `06-交付/01-多格式书架与续读/审查报告-00-首版.md` | 首版双轴审查（绿地降级） | 2026-09-17 |
| 一致性对账 | 00 | `06-交付/01-多格式书架与续读/一致性对账-00-首版.md` | 首版代码↔文档对账 | 2026-09-17 |
| 交付报告 | 00 | `06-交付/01-多格式书架与续读/交付报告-00-首版.md` | 首版交付 | 2026-09-17 |

收敛型（本批次内）：`01-需求/01-多格式书架与续读/领域模型.md`、`跨端共享需求.md`、`02-原型/01-多格式书架与续读/原型说明.md` + `原型图.html`、`04-落地计划/01-多格式书架与续读/架构审计.md` / `回滚方案.md` / `任务拆分.md`、`05-实现与测试/01-多格式书架与续读/测试计划.md`。

- **追加型（批次内）**：本批次的第 NN 版，每次修订**新建下一个编号**，历史不可变。新增 = 新建下一个序号文件 + 上表追加一行。
- **收敛型（批次内）**：表达**本批次结束时的**当前真相，批次内原地更新；**非首批次开工时应从上一批次同名文件继承**再演进。

### 3.2 全局单例产物（跨批次共享，无批次目录）

| 产物 | 作用域 | 表达什么 |
|---|---|---|
| `03-架构/待查证问题.md` / `事实纪要.md` / `设计约束.md` / `候选方案.md` / `方案对比.md` / `跨端共享层.md` / `方案草案.md` | **全项目共享** | 全项目架构真相。新批次**参考**它做设计；**它改动必须文档 + 代码 + 所有活跃批次相关产物同步** |
| `00-入口/文档导航.md` | 全项目 | 本文件（批次清单 + 读取入口） |
| `99-来源与日志/运行日志.jsonl` | 全项目 | 全程增量日志 |

## 4. 主线读取顺序

新接手者按此顺序建立全局认知：

1. 本文（导航）→ 看第 3 节确定**当前批次**
2. `03-架构/方案草案.md` + `03-架构/跨端共享层.md`（**架构基线，全局共享**）
3. 进当前批次：`01-需求/<批次>/需求-*.md`（**按序号从小到大全部读**）→ `领域模型.md`
4. `02-原型/<批次>/原型说明.md` + `原型图.html`
5. `04-落地计划/<批次>/变更清单-*.md`（按序号全部读）→ `任务拆分.md`
6. `05-实现与测试/<批次>/测试计划.md`

（本工程 `<批次>` = `01-多格式书架与续读`）

## 5. 子 agent 分派口径

给子 agent 分派任务时使用以下格式：

```text
任务：<任务编号 - 名称>
批次：<NN-需求名>

必读：
1. docs/<feature>/00-入口/文档导航.md
2. docs/<feature>/03-架构/方案草案.md（架构基线，全局共享）
3. docs/<feature>/01-需求/<批次>/ 下全部 需求-*.md（必须全读，需求是追加累加的）
4. docs/<feature>/02-原型/<批次>/原型说明.md（涉及界面时必读，配套 原型图.html）
5. docs/<feature>/04-落地计划/<批次>/ 下最新 变更清单-*.md
6. 对应拆分产物

边界：
- 只处理被分配任务，且只动分配批次。
- 不改动闸门已确认的契约；不改动 `03-架构/`（架构改动须走全项目同步流程）。
- 不改动任何已编号的追加型产物（历史不可变）；需要修正就新建下一个编号。
- 不改动任何已冻结的历史批次；需要变更就走新批次。
- 如发现产物与本文冲突，先停下报告。
```

## 6. 确认记录与来源材料

这些用于追溯过程，不作为开发唯一入口：

- `99-来源与日志/` 下的原始需求/设计稿/历史方案
- `99-来源与日志/运行日志.jsonl` 增量日志
