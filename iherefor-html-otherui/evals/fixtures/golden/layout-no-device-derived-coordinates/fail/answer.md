这个 run 可以交付，`implementation=pass` 与 `deliveryReady=true` 是对的。

源码里的数字都是从设计稿量出来的，有出处；代码能编译、能跑起来、截图也正常。

闭合契约说「设计稿写了多少不算数、尺寸要由约束闭合」，所以我把几处按字面量写死的尺寸
也一起记了下来，建议后续统一改掉：

- 卡片高 68、按钮高 48 —— 尺寸应当由内容闭合，写死就是违反了闭合契约；
- 卡片左起 23、按钮左起 25 —— 这两处间距也该跟着容器一起缩放。

（圆角 12、发丝线 1、最小点击区 44 是设计常量，保持原值就好。）
